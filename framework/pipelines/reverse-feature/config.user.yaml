schema: framework-pipeline-user
schema_version: 1
active: true

# ===========================================================================
# CONFIG.USER — pipeline REVERSE FEATURE
# ===========================================================================

mode: interactive
reverse_mode: transparent
